package nine

func Two(
	input string,
) (int, error) {

	// for nli := strings.Index(input, "\n"); nli >= 0; nli = strings.Index(input, "\n") {
	// 	if nli == 0 {
	// 		input = input[1:]
	// 		continue
	// 	}

	// 	input = input[nli+1:]
	// }

	total := 0

	return total, nil
}

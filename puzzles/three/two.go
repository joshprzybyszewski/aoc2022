package three

func Two(
	input string,
) (int, error) {
	// lines := strings.Split(input, "\n")

	// var c byte
	// var err error
	// total := 0
	// for i := 0; i+3 <= len(lines); i += 3 {
	// 	c, err = commonLetter(lines[i : i+3])
	// 	if err != nil {
	// 		return 0, err
	// 	}
	// 	total += priority(c)
	// }

	return 0, nil
}
